# Dockerfile гарантирует наличие ffmpeg в образе (nixpacks иногда не прокидывал его в runtime).
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
