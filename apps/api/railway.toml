# Явно задаём Dockerfile (с FFmpeg), чтобы Railway не использовал Nixpacks.
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
